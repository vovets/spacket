#pragma once

#include <cstdint>

constexpr std::size_t BUFFER_MAX_SIZE = 40;
// constexpr std::size_t BUFFER_MAX_SIZE = 300;
constexpr std::size_t POOL_NUM_BUFFERS = 4;
constexpr std::size_t EXECUTOR_CAPACITY = 1;
constexpr std::size_t PROC_MAX_SIZE = 16;
constexpr std::size_t NUM_PROC = 2;
